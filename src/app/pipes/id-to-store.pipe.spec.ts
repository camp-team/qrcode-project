import { IdToStorePipe } from './id-to-store.pipe';

describe('IdToStorePipe', () => {
  it('create an instance', () => {
    const pipe = new IdToStorePipe();
    expect(pipe).toBeTruthy();
  });
});
